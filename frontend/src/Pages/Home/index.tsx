import { ReactNode } from "react";
import Navbar from "../../components/Navbar";

const Home = (): ReactNode => {
    return (
        <>
            <Navbar/>
        </>
    );
};

export default Home;